language: node_js
node_js:
  - stable
  - '10'
  - '8'
before_install:
  - curl -o- -L https://yarnpkg.com/install.sh | bash -s -- --version 1.16.0
  - export PATH="$HOME/.yarn/bin:$PATH"
cache: yarn
before_script:
  - yarn run test
script:
  - yarn run build
